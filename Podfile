# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftExamGit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftExamGit
  pod 'SDWebImage', '~> 4.0'

  target 'SwiftExamGitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftExamGitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
